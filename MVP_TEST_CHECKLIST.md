# VIP 宠护小程序 MVP 跑通测试清单

## 1. 本地验证

已配置根目录测试命令：

```bash
npm test
```

当前已覆盖云函数单元测试：

- `auth` 登录和角色切换
- `order` 创建订单、强制打卡校验
- `homeSecurity` 门锁密码加密、解锁时间窗校验和日志记录

## 2. 微信云开发准备

### 2.1 配置云环境 ID

在微信开发者工具中创建云开发环境后，修改：

```js
// miniprogram/envList.js
const envList = [
  { envId: '你的云环境ID', alias: '开发环境' }
]
```

### 2.2 创建云数据库集合

在云开发控制台创建以下集合：

```text
users
pets
home_security
staff_profiles
orders
payments
track_logs
checkin_logs
unlock_code_logs
order_incidents
admin_operation_logs
```

### 2.3 部署云函数

需要部署：

```text
auth
pet
homeSecurity
staff
order
payment
track
checkin
incident
admin
initData
```

## 3. 初始化管理员

1. 打开小程序。
2. 进入登录页。
3. 点击「开发初始化」。
4. 点击「检查集合」，确认集合都已存在。
5. 点击「把当前账号设为管理员」。
6. 返回登录页重新登录。
7. 进入角色选择页，应看到管理端入口。

也可以点击「生成演示数据」创建：

- 当前账号管理员角色
- 一个演示宠物
- 一个已支付演示订单

注意：`pages/dev/init/index` 仅用于开发期，上线前应从 `app.json` 移除。

## 4. 宠物主端跑通

1. 登录。
2. 选择「宠物主端」。
3. 进入「宠物档案」。
4. 新增宠物：名称、品种、体重、备注。
5. 进入「家庭安防」。
6. 保存门锁密码、钥匙位置、入户说明。
7. 进入「预约下单」。
8. 选择宠物和服务类型。
9. 填写地址、开始时间、结束时间。
10. 点击「试算价格」。
11. 点击「创建订单」。
12. 在订单详情点击「Mock 支付」。
13. 确认订单状态变为 `paid`。

## 5. 员工端跑通

1. 登录。
2. 选择「员工端」。
3. 进入「入驻认证」。
4. 填写姓名、手机号、服务城市、服务区域。
5. 提交认证。
6. 等待管理员审核通过。
7. 审核通过后重新进入「我的任务」。
8. 查看管理员派给自己的订单。
9. 进入任务详情。
10. 点击「进入服务」。
11. 点击「开始服务」。
12. 在服务时间窗口内点击「查看限时门锁密码」。
13. 点击「上传当前定位」。
14. 按强制节点打卡：
    - 遛狗：`enter_door`、`leash_on`、`return_home`、`leave_door`
    - 喂养：`enter_door`、`feed`、`water`、`pet_status`、`leave_door`
15. 点击「完成服务」。
16. 确认订单变为 `completed`。

## 6. 管理端跑通

1. 登录管理员账号。
2. 选择「管理端」。
3. 查看管理首页统计。
4. 进入「员工审核」。
5. 审核员工通过。
6. 进入「订单管理」。
7. 找到 `paid` 订单。
8. 点击派单。
9. 选择已审核员工。
10. 确认订单变为 `assigned`。
11. 服务开始后进入订单详情。
12. 查看轨迹点、打卡数量、门锁密码访问日志。
13. 进入「证据链」确认数据存在。
14. 进入「异常事件」处理 SOS。

## 7. 安全验证

建议至少验证：

- 非管理员不能调用 `admin` 云函数。
- 员工不能查看未分配给自己的订单。
- 员工不能查看其他订单的门锁密码。
- 员工不能在服务窗口外查看门锁密码。
- 每次门锁密码查看失败也会写入 `unlock_code_logs`。
- 数据库 `home_security` 中不出现明文门锁密码。
- 未完成强制打卡时不能完成订单。

## 8. 当前限制

- 支付仍为 Mock 支付。
- 管理端是小程序内页面，不是 PC Web 后台。
- 图片水印还未实现。
- GPS 是手动上传当前定位，未实现后台持续队列。
- 员工资质文件上传字段已预留，但页面暂未做完整上传流程。
- 开发初始化页上线前必须移除。
